<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/cadastro_ejs.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Document</title>
</head>
<body>
            <!-- Exibir mensagens de flash -->
    <% if (success && success.length) { %>
        <div id="flash-message" class="alert alert-success mx-4">
            <%= success[0] %>
        </div>
    <% } %>
    <% if (error && error.length) { %>
        <div id="flash-message" class="alert alert-danger mx-4">
            <%= error[0] %>
        </div>
    <% } %>
    
    <div class="form-container">
        <h2 class="text-left text-2xl font-bold p-4">Cadastre seu sítio para continuar</h2>
        <hr class="gradient-hr mb-4">
        <form action="/createSitio" method="post">
            <input type="hidden" name="id_user" value="<%= userId %>"> <!-- Campo oculto com o ID do usuário -->
            <input type="text" id="nome" name="nome" placeholder="Nome do Sítio" class="form-control" required>
            <input type="text" id="rua" name="rua" placeholder="Rua" class="form-control" required>
            <input type="text" id="bairro" name="bairro" placeholder="Bairro" class="form-control" required>
            <input type="text" id="cidade" name="cidade" placeholder="Cidade" class="form-control" required>
            <input type="text" id="numero" name="numero" placeholder="Número" class="form-control" required>
            <button type="submit" class="btn-custom">Cadastrar Sítio</button>
        </form>

    <p class="mt-2">Já tem uma conta? <a href="/login" class="text-[#6691FF]">Conecte-se</a></p>

    </div>
    <img src="/imgs/camarize.png" class="w-48 mt-4" alt="">
</body>
</html>

