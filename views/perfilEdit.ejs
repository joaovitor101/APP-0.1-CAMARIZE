<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/cadastro_ejs.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Editar Perfil</title>
</head>
<body>
    <div class="form-container">
        <h2 class="text-left text-2xl font-bold p-4">Edição do meu perfil</h2>
        <hr class="gradient-hr mb-4">

        <form action="/perfil/update" method="POST" enctype="multipart/form-data">
            <!-- Exibe foto atual ou padrão -->
            <div class="mb-4">
                <img src="<%= user.foto_perfil ? '/uploads/' + user.foto_perfil : '/imgs/profile-icon.jpg' %>" alt="Foto de Perfil" style="width: 150px; border-radius: 50%;">
                <input type="file" name="foto_perfil" accept="image/*" class="mt-2">
            </div>

            <!-- Nome -->
            <div class="mb-4">
                <input type="text" name="nome" value="<%= user.nome %>" class="form-control" placeholder="Nome">
            </div>

            <!-- E-mail -->
            <div class="mb-4">
                <input type="email" name="email" value="<%= user.email %>" class="form-control" placeholder="E-mail">
            </div>

            <!-- Senha -->
            <div class="mb-4">
                <input type="password" name="senha" class="form-control" placeholder="Senha">
            </div>

            <button type="submit" class="btn-custom">Atualizar</button>
        </form>
        
        <hr class="gradient-hr mb-4 mt-4">
    </div>
    <img src="/imgs/camarize.png" class="w-48 mt-4" alt="Camarize Logo">
</body>
</html>
